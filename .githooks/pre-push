#!/usr/bin/env bash
# Keep the local push gate cheap; GitHub Actions runs the canonical full CI.
# Enable once: git config core.hooksPath .githooks
set -euo pipefail

root=$(git rev-parse --show-toplevel)
cd "$root"
exec cargo fmt --all --check
