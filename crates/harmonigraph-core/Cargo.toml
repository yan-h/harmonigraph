[package]
name = "harmonigraph-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Just-intonation pitch math, Tonnetz lattice coordinates, and note spelling. No dependencies."
readme = "README.md"
# Deliberately NOT the workspace's GPL-3.0-or-later: this crate is the one
# reusable library in the workspace, and much of its pitch math descends from
# the MIT-licensed midi_lattice v1. See README.md for the rationale, and
# ci.sh for the guard that keeps this crate dependency-free.
license = "MIT OR Apache-2.0"

[dependencies]
